- var scriptPath = "./js/";

-
    var scripts = {
        vendor: "vendor.min.js",
        main: "main.min.js",
    }

each script in scripts
    if script
        script(defer, type="module", src=`${scriptPath}${script}`)
